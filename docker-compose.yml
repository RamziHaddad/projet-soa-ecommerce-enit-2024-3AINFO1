version: '3'

services:
  reviews-service:
    image: ahmeddhibi/reviews-service:latest
    container_name: reviews-service
    ports:
      - 8088:8088
    networks:
      - global-network
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ConnectionStrings__DefaultConnection: "Host=bank-service-db_bank-1;Database=reviewdb;Username=postgres;Password=azerty"


networks:
  global-network:
    external: true
